<?xml version="1.0" encoding="utf-8"?>
<Project>
  <PropertyGroup>
    <!-- Shared source link / deterministic build settings -->
    <LangVersion>7.3</LangVersion>
    <PathMap>$(MSBuildProjectDirectory)=Tai</PathMap>
  </PropertyGroup>
</Project>
